let points = null;
console.log(points, points + 3, `Your points are: ${points}`);

let undefined_points;
console.log(undefined_points, undefined_points + 3, `Your undefined points are: ${undefined_points}`);

